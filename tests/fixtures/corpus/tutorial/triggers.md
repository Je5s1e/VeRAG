# Quantifiers and triggers

A trigger controls when the solver instantiates a forall quantifier. Select a
term containing the bound variable, such as seq[i], and ensure the relevant term
exists in the proof context. A missing trigger can prevent an assertion proof.
