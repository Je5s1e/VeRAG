# Integer overflow

Executable u64 arithmetic can overflow. Prove an upper bound before addition.
Mathematical int and nat are used in specifications, unlike bounded machine
integers. A Seq<int> preserves its element type in the documentation.
